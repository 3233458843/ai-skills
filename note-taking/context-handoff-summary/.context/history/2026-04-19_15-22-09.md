【续聊上下文】
目标：把 context-handoff-summary 优化成低 token 消耗的续聊 skill。
约束：只保留最小必要上下文；默认不解释过程；默认不回显完整摘要；保存到 .context/current.md。
进度：skill 已精简为低 token 版，固定 6 行格式，默认只读 current.md，收尾默认只回“已更新上下文”。
待办/卡点：后续使用时按口令触发即可；若需要摘要正文需显式要求“把摘要也发我”。
关键资料：skill=C:\Users\lxw32\.hermes\skills\note-taking\context-handoff-summary；用法=.context/USAGE.txt；当前文件=.context/current.md。
下次直接执行：新对话加载 skill 并读取 .context/current.md；结束时说“收尾并更新上下文”。
