【续聊上下文】
目标：建立一个可在每次对话结束后更新的精简上下文 skill。
约束：要求最精简但最大化保留续聊所需信息；上下文需保存到该 skill 的 .context 文件夹。
进度：已创建并升级 context-handoff-summary skill，加入读取旧上下文、增量更新、覆盖 current.md、保存 history 快照的规则。
待办/卡点：后续实际使用时，需要在每次阶段结束或用户要求收尾时执行一次摘要生成与文件写入。
关键资料：skill 路径=C:\Users\lxw32\.hermes\skills\note-taking\context-handoff-summary；上下文目录=.context；当前文件=.context/current.md。
下次直接执行：加载 context-handoff-summary 后，基于当前对话生成新摘要并覆盖写入 .context/current.md，同时保存一份 history 快照。
