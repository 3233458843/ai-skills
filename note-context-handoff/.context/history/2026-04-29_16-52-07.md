【续聊上下文】
目标：继续完善 XST_Demo 的 XST 掌静脉驱动，使其通过统一 xst_ops_t/user_ctx/porting 层兼容 STM32 HAL、标准库/GD32、ESP32。
约束：严格遵循项目 skill：先理解、最小改动、平台无关核心；xst_drv.c 不直接依赖 HAL/标准库/ESP-IDF；平台差异放 port 层；回答直接、 factual。
进度：已读取项目 skill；xst_ops_t 已改为带 user_ctx；xst_drv 内部加入静态 rx/tx storage 和 lwrb 初始化；新增 xst_write_rx_data/xst_read_rx_data；新增 MULTI_PLATFORM_PORTING.md；已撤销直接暴露 lwrb 写指针方案；清理 xst_porting.c 中错误 HAL 示例。
待办/卡点：用户正在重新思考 HAL 与标准库的共性配置；重点应围绕统一平台上下文、ops 注册、平台 ISR 收到数据后调用 xst_write_rx_data，而不是让 HAL 直接写 rx_buf。
关键资料：核心文件 bsp/xst_driver/xst_drv.h、xst_drv.c、porting/xst_porting.c、porting/xst_porting.h、MULTI_PLATFORM_PORTING.md；构建环境无 cmake，但 arm-none-eabi-gcc -fsyntax-only 可用。
下次直接执行：先按本上下文继续，若修改代码则保持 xst_drv 平台无关；优先设计统一 port context/ops 模板，再分别给 HAL/标准库/GD32/ESP32 示例。
