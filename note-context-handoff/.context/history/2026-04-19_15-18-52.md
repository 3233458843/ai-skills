【续聊上下文】
目标：把 context-handoff-summary 做成尽可能省事的续聊上下文管理 skill。
约束：上下文需最精简但保留最大化有效信息；结束对话时可用一句口令触发更新；文件保存到 skill 下的 .context 目录。
进度：已为 skill 增加便捷触发口令规则，并新增 .context/USAGE.txt 说明；推荐口令为“收尾并更新上下文”。
待办/卡点：后续实际使用时，只需在阶段结束时说触发口令即可执行更新与保存。
关键资料：skill=C:\Users\lxw32\.hermes\skills\note-taking\context-handoff-summary；当前上下文=.context/current.md；说明文件=.context/USAGE.txt。
下次直接执行：新对话先读取 .context/current.md；结束时直接说“收尾并更新上下文”。
